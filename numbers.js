let x = 33;
let str = 'abc';

console.log(x*str);

/*
parseInt() :
The first parameter is the string that you'd like to convert into a number. 
The second argument is the radix that will be used in the conversion.
You can use 10 or 2 as radix
radix 10 represents the decimal system
radix 2 represents binary
*/
let y ="23";
console.log(Number.parseInt(y,10));