//the code will execute at least once
// the code executes first, then checks the condition
// if condition is not met, returns the statement


let age = 5;

do{
    console.log(age);
    age++;
} while(age < 7);